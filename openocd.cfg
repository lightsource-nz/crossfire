source [find interface/cmsis-dap.cfg]
adapter speed 5000

# rp2040.cfg defaults to USE_CORE SMP, exposing both cores as an hwthread RTOS
# group; launch.json's "rtos": "hwthread" must match this
source [find target/rp2040.cfg]
